export type Health={ok:boolean;configured:boolean;upstream?:unknown};export async function apiFetch<T>(path:string,init?:RequestInit):Promise<T>{const base=process.env.NEXT_PUBLIC_API_URL;if(!base)throw new Error("API is not configured");const r=await fetch(base.replace(/\/$/,"")+path,{...init,headers:{"Content-Type":"application/json",...(init?.headers||{})},cache:"no-store"});if(!r.ok)throw new Error(`API ${r.status}`);return r.json();}