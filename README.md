# La😂Print Web

Drift-backed automated trading terminal.

The wallet layer connects users to their own Solana wallet. Funding remains gated until the production Drift transaction builder is connected; the UI never claims a deposit succeeded without a signed transaction.
