# La😂Print Web

Drift-backed frontend for the La😂Print automated trading terminal.

UI routes are implemented. Production wallet signing, Drift reads, engine state and live execution remain gated until backend contracts are connected.
