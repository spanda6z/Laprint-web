"use client";import Link from "next/link";import {useSearchParams} from "next/navigation";import {useState} from "react";
export default function Setup(){const q=useSearchParams();const tier=q.get("tier")==="spot"?"spot":"leverage";const [lev,setLev]=useState(3);return <main className="min-h-screen bg-[#070707]"><div className="mx-auto max-w-3xl px-5 py-10"><Link href="/tier" className="text-sm text-zinc-500">← Tier</Link><div className="mt-20 mono text-xs text-zinc-600">03 / AUTOPILOT</div><h1 className="mt-4 text-5xl font-semibold">Configure {tier}.</h1><div className="mt-10 rounded-2xl border border-white/10 p-6"><label className="text-xs text-zinc-500">MARKET</label><div className="mt-3 rounded-xl border border-white/10 p-4">BONK-PERP <span className="float-right text-xs text-zinc-600">DRIFT</span></div>{tier==="leverage"&&<><div className="mt-8 flex justify-between text-sm"><span>Leverage</span><span>{lev.toFixed(1)}x</span></div><input className="mt-4 w-full" type="range" min="1" max="10" step=".1" value={lev} onChange={e=>setLev(+e.target.value)}/><div className="mt-8 grid grid-cols-2 gap-3"><div className="rounded-xl border border-white/10 p-4"><span className="text-xs text-zinc-600">LIQUIDATION</span><div className="mt-2 text-sm text-zinc-400">Calculated from live Drift account</div></div><div className="rounded-xl border border-white/10 p-4"><span className="text-xs text-zinc-600">MAX LOSS</span><div className="mt-2 text-sm text-zinc-400">User configurable</div></div></div></>}<button className="mt-8 w-full rounded-xl bg-white px-5 py-4 text-sm font-bold text-black">Save configuration</button></div></div></main>}